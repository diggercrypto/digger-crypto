name: Build and deploy Jekyll site on: push: branches: [ "main" ] permissions: contents: read pages: write id-token: write jobs: build: runs-on: ubuntu-latest steps: - uses: actions/checkout@v4 - name: Build with jekyll/builder run: | docker run -v ${{ github.workspace }}:/srv/jekyll -v ${{ github.workspace }}/_site:/srv/jekyll/_site jekyll/builder:latest /bin/bash -c "chmod -R 777 /srv/jekyll && jekyll build --future" - name: Upload Pages artifact uses: actions/upload-pages-artifact@v1 with: path: ./_site deploy: runs-on: ubuntu-latest needs: build steps: - uses: actions/configure-pages@v3 - uses: actions/deploy-pages@v3
